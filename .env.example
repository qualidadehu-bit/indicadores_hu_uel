# Frontend (Vite) — copie para `.env` ou `.env.local`
# URL do Worker (dev: npm run worker:dev — porta padrão 8787)
VITE_WORKER_URL=http://localhost:8787

# Worker (Wrangler) — veja worker/.dev.vars.example e gas/Code.gs para API_SECRET / GAS_WEBAPP_URL / GAS_SECRET
