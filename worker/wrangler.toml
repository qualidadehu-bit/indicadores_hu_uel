name = "indicadores-hu-api"
main = "src/index.ts"
compatibility_date = "2024-05-01"

# Defina GAS_WEBAPP_URL aqui ou via painel Cloudflare (Settings → Variables).
# Segredo compartilhado Worker ↔ GAS: wrangler secret put GAS_SECRET
[vars]
# GAS_WEBAPP_URL = "https://script.google.com/macros/s/SEU_DEPLOY/exec"
